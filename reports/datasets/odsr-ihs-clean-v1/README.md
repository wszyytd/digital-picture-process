# ODSR-IHS 首版清洗记录

日期：2026-09-17。用途：真实低机位室内 14 类目标检测基线。

| 项目 | 结果 |
|---|---:|
| 原图 / XML | 6000 / 6000 |
| 原 XML 目标数 | 16465 |
| 排除图片 | 4 |
| 保留图片 | 5996 |
| 修正类别的目标 | 35 |
| 保留目标 | 16447 |
| 训练 / 验证 / 测试图片 | 4796 / 240 / 960 |

原 XML 目标数与论文简介中的 16409 不同；本次按实际文件统计。4 张排除图片全部属于原训练集，验证和测试图片数不变。原标签 TXT 存在异常类别漏标，因此所有标签从 XML 重新生成。

排除 ID：004235、005107、005214、005423。其余类别修正详见 configs/datasets/odsr-ihs-cleaning.json。正常类别编号保持源 TXT 顺序。

发现完全相同的图片文件 002804、002805；它们在同一集合，首版保留并记录。没有发现跨集合的完全相同图片文件。没有完成近重复、房间和序列级泄漏排查，不应据此声称测试集场景完全独立。

全部图片解码、实际尺寸与 XML 尺寸匹配、坐标有效、划分一致性检查通过。独立重读全部生成文件，核对 5996 对图像/标签 SHA256；16447 个归一化框反算回 XML 坐标误差小于 0.00001 像素。抽看修正后的 004774（柜子）、002744（窗帘）完整标注预览，位置正常。并非对全部普通标签进行了人工审核。

数据输出：data/odsr-ihs。该目录中的 manifest.json、summary.json、cleaning.json、划分清单及 review 预览用于本地/服务器复现。数据不提交 Git。版本化清洗汇总见本目录 summary.json；操作命令见 docs/odsr-ihs.md。

验证集袜子仅有 6 个标注目标；逐类指标可能波动较大。数据没有人、宠物和机器人类别，不能作为全部项目需求已满足的依据。尚未执行此数据集上的正式训练。
